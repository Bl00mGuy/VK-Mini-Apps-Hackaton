import * as React from 'react';
import type { HasComponent, HasRootRef } from '../../../types';
export interface FormItemTopLabelProps extends React.AllHTMLAttributes<HTMLElement>, HasRootRef<HTMLElement>, HasComponent {
    /**
     * Многострочный вывод заголовка.
     * TODO [>=7]: удалить и вседа брать из контекста
     */
    multiline?: boolean;
}
/**
 * Отвечает за отрисовку заголовка поля. По умолчанию компонент представлен тегом `label`, если передано свойство `htmlFor`.
 * Можно переопределить через свойство `Component`.
 *
 * @since 6.1.0
 *
 */
export declare const FormItemTopLabel: React.FC<FormItemTopLabelProps>;
//# sourceMappingURL=FormItemTopLabel.d.ts.map