import * as React from 'react';
import type { HasRootRef } from '../../types';
import type { TappableProps } from '../Tappable/Tappable';
import { CheckboxInput, type CheckboxInputProps } from './CheckboxInput/CheckboxInput';
export interface CheckboxProps extends Omit<CheckboxInputProps, 'getRootRef'>, HasRootRef<HTMLLabelElement>, Pick<TappableProps, 'hoverMode' | 'activeMode' | 'hasHover' | 'hasActive' | 'focusVisibleMode'> {
    description?: React.ReactNode;
    titleAfter?: React.ReactNode;
}
/**
 * @see https://vkcom.github.io/VKUI/#/Checkbox
 */
export declare const Checkbox: {
    (props: CheckboxProps): React.ReactNode;
    Input: typeof CheckboxInput;
};
//# sourceMappingURL=Checkbox.d.ts.map