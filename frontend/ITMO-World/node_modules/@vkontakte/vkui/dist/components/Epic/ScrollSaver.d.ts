import * as React from 'react';
import type { HasChildren } from '../../types';
export interface ScrollSaverProps extends HasChildren {
    initialScroll?: number;
    saveScroll: (this: void, scroll: number) => any;
}
/**
 * @see https://vkcom.github.io/VKUI/#/ScrollSaver
 */
export declare const ScrollSaver: ({ children, initialScroll, saveScroll, }: ScrollSaverProps) => React.ReactNode;
//# sourceMappingURL=ScrollSaver.d.ts.map