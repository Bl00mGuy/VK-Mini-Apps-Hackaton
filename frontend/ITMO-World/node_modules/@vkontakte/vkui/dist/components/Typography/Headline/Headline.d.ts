import { type TypographyProps } from '../Typography';
export interface HeadlineProps extends TypographyProps {
    level?: '1' | '2';
}
/**
 * Используется для подзаголовков.
 *
 * @see https://vkcom.github.io/VKUI/#/Headline
 */
export declare const Headline: ({ className, level, Component, normalize, inline, ...restProps }: HeadlineProps) => React.ReactNode;
//# sourceMappingURL=Headline.d.ts.map